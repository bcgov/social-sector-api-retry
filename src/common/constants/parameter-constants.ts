const CONTENT_TYPE = 'application/json';
const urlMax = 32767; // max for a 16-bit signed integer, most urls are much shorter
const maxFileSizeBytes = 5242880; // 5MiB

export { CONTENT_TYPE, urlMax, maxFileSizeBytes };
