apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-social-retry-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512MiB
  storageClassName: netapp-file-standard