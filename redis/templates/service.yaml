apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.deployment.name }}"
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app: "{{ .Values.deployment.name }}"
    deployment: "{{ .Values.deployment.name }}"